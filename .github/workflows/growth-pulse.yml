name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-celo-clicker-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify celo-clicker-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/celo-clicker-sdk-check

      - name: Initialize project
        working-directory: /tmp/celo-clicker-sdk-check
        run: npm init -y

      - name: Fetch celo-clicker-sdk from registry
        working-directory: /tmp/celo-clicker-sdk-check
        run: npm install celo-clicker-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/celo-clicker-sdk-check
        run: node -e "const sdk = require('celo-clicker-sdk'); console.log('Scheduled health check: celo-clicker-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/celo-clicker-sdk-check
        run: npm ls --all
